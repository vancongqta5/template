export interface DiscountModel{
      forEach(arg0: (res: any) => void): unknown;
      id : number;
      createdDate: number;
      updatedDate: number;
      discountName: string;
      discountPercent: number;
}